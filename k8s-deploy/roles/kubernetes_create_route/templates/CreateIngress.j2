kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: {{ item.deploy_name }}
  namespace: {{ item.namespace }}
spec:
  rules:
    - http:
        paths:
          - path: 
            backend:
              serviceName: {{ routes.name }}
              servicePort: {{ routes.port }}
