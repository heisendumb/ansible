group: "mediawiki"
app_name: "mediawiki"
number_replicas: 1

apps:
  - name: "{{app_name}}"
    deploy_name: "{{app_name}}"
    namespace: "{{group}}"
    replicas: "{{number_replicas}}"
    helm:
      - name: "mediawiki"
        version: "6.2.0"
        chart: "https://kubernetes-charts.storage.googleapis.com"
    env_vars: "values.yml"
    route:
